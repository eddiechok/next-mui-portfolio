import { Typography } from '@mui/material';

import { ContentSection, Flex } from '@/components/ui';

export const Hero = () => {
  return (
    <ContentSection>
      <Flex>
        <Typography color="text.secondary">123123</Typography>
      </Flex>
    </ContentSection>
  );
};
